{
  "name": "treolympus",
  "version": "1.0.0",
  "private": true,
  "description": "Self-contained bilingual landing page — GitHub and Vercel deployment package",
  "type": "module",
  "engines": { "node": "22.x" },
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "npm run check",
    "build": "node scripts/check.mjs && node scripts/build.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "start": "node scripts/serve.mjs"
  }
}
